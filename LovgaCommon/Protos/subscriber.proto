syntax = "proto3";
import "common.proto";

option csharp_namespace = "LovgaCommon";

package broker.subscriber;

service Subscriber{
  rpc Subscribe (SubscribeRequest) returns (broker.reply.Reply);
}

message SubscribeRequest{
  string url = 1;
  string topic = 2;
}