syntax = "proto3";
import "common.proto";

option csharp_namespace = "LovgaCommon";

package broker.client;

service ConsumerClient{
  rpc NotifyConsumer (NotifyRequest) returns (broker.reply.Reply);
}

message NotifyRequest{
  string test = 1;
}